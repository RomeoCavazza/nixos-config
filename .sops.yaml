# Both the previous admin_tco and host_legion private keys were lost in the
# 2026-07-11 disk wipe (neither was backed up, by design). Re-keyed with a
# single fresh host key; add a second, separately-stored admin recipient
# once one exists again.
keys:
  - &host_legion age1akjpsw3d8kac2fxnprey8qh3r99m58hp8jawmm96juatme4h3q8q73sru0

creation_rules:
  - path_regex: secrets/.*\.yaml$
    key_groups:
      - age:
          - *host_legion
